Description:
    Generates a deploy.rb that works better with rockin.
    Pulls application name from the Rails app name and the repository 
    from the git config for remote origin.

Example:
    rails generate rockin:install 255.255.255.255

    This will create:
        config/deploy.rb
